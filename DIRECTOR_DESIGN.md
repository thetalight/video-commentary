# AI 导演 v2：证据优先的影视解说流水线

## 问题根因

旧流程把局部字幕直接压缩为剧情节拍，再让同一个模型补人物、动机和因果。字幕只提供对白，通常不会标明“这是演员采访”“这是片中节目”或“当前说话人是谁”。当提示词同时要求文案饱满、必须点名人物和达到固定字数时，模型会用先验知识填补缺口。错误一旦进入剧情节拍，后续蓝图、分章和补写会继续放大。

《楚门的世界》开头 50–115 秒就是典型案例：字幕包含登山、断腿和节目访谈，但这只是节目开场素材，不是楚门亲身经历。

## 参考的开源架构

- [Yapper](https://github.com/yuchia329/yapper)：采用可恢复的多阶段工件，以及 MAP（理解）→ REDUCE（写稿）→ runtime budget 的职责拆分。
- [FunClip](https://github.com/modelscope/FunClip)：字幕时间戳是选片和剪辑的权威来源。
- [VideoSEAL](https://github.com/Echochef/VideoSEAL)：把规划者和证据裁判分开，避免写稿模型自我批准。
- [Movie Narrator](https://github.com/zcbacxc/movie-narrator)：候选生成后再通过评分与成片质量门。

本项目借鉴上述架构原则，没有直接复制受限制许可证项目的实现代码。

## 新流水线

```text
字幕时间轴
  ↓
MAP：原子剧情节拍 + narrative_layer
  ↓
过滤 framing / 片头 / 片尾 / 广告
  ↓
整集因果蓝图
  ↓
REDUCE：分章导演稿
  ↓
证据化补写（句子 + 字幕原文 + 目标片段）
  ↓
独立事实裁判（每段必须给出字幕原文）
  ↓
本地逐字、时间窗口和结构校验
  ↓
edit.json
```

## 核心数据约束

### 叙事层

每个剧情节拍必须属于以下一种：

- `main`：当前主线。
- `framing`：演员采访、节目导语、幕后说明、预告或主线前的片中节目；不进入导演稿。
- `in_world_media`：角色正在观看、且会推动主线的电视或节目内容。
- `flashback`：回忆。
- `uncertain`：字幕不足以确认叙事层。

禁止把 `framing`、`in_world_media` 或 `flashback` 中的人物动作自动归给主角。

### 证据化补写

每条补写必须包含：

- `target_segment_index`：要追加到哪个非原声片段。
- `evidence_quote`：从该片段附近字幕逐字复制的原文。
- `sentence`：只表达这条原文直接支持的事实。

程序会在目标片段前后 12 秒的字幕中逐字查找 `evidence_quote`。找不到就丢弃，不能用无证据句子凑 30% 字数。

### 独立事实裁判

裁判必须为每个片段返回 verdict：

- `supported`：原稿是否全部受证据支持。
- `evidence_quote`：逐字字幕证据。
- `corrected_narration`：越界时的保守改写；无法改正则为空并删除片段。

漏审、伪造证据、跨时间引用、人名不在字幕、把节目片段写成主角经历，都会在本地质量门被拒绝。

## 可靠性策略

- 43 分钟影片约拆为 10 个章节，控制单次 JSON 体积。
- `beats.json`、`blueprint.json`、每章 draft/verdict/checked 和最终稿均落盘到 `director/runs/{input_revision}/`。
- 检查点按字幕、模型、风格和提示词版本生成输入指纹；输入变化时自动进入新运行目录。
- JSON 截断时扩大额度重试；仍截断则恢复已经闭合的完整片段。
- 补写采用累加，不覆盖已有章节。
- 字幕已经存在时，重试从 AI 导演的最后有效章节继续；成功稿上的“重新生成”会清理本轮检查点并重新创作。
- 30% 口播时长仍是硬指标，但不能以牺牲事实正确性为代价。

## 回归样例

《楚门的世界》开头的登山对白必须被标记为 `framing` 并从主线节拍移除。若写稿出现“楚门双腿折断仍坚持登山”，事实裁判必须改为“节目开场播放登山片段”等保守说法，或直接删除；字幕不存在的 `Eve`、`Eudon` 等姓名同样不能通过证据门。
