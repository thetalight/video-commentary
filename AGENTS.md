# 视频解说应用

桌面应用负责完整流程：**下载 → 字幕 → AI 导演 → 审稿 → 配音 → ffmpeg 成片**。

## AI 导演

- `edit.json` 与 `commentary.md` 由桌面应用调用设置中的 AI 来源自动生成（默认阿里云百炼，可切换本地 Ollama）。
- API Key 缺失、服务不可用、模型错误或输出校验失败时，应在 UI 中显示错误并提供“重试 AI 导演”，不启用任何外部写稿兜底。
- 导演仅依据字幕，采用“剧情节拍（MAP）→ 导演脚本（REDUCE）”两阶段生成。
- 自动跳过片头、片尾、演职员表、中间广告、赞助口播、下载引导和下集预告。
- 导演脚本必须符合任务目录的 `SCHEMA.md` 与 `STYLE.md`。

## 开发与诊断约束

- 不要直接运行 ffmpeg、yt-dlp、whisper 或 edge-tts；这些由桌面应用负责。
- 不要修改任务的 `META.json` 或字幕原文。
- 诊断解说质量时可读取 `META.json`、`TRANSCRIPT.compact.txt`、`TRANSCRIPT.srt`、`edit.json` 和 `commentary.md`。
- 不要把原片当作导演输入；导演阶段只使用字幕时间戳和文本。
- 片段数量不设上限，每段至少 0.8 秒；普通选片 4–20 秒，原声选片 4–12 秒；按钩子、证据、兑现组织叙事，完整覆盖关键因果，避免逐句复述和 AI 套话。
- 纯解说口播时长必须达到原视频时长的 30%，按每秒约 4 个汉字计算；保留原声时长不计入该比例。
